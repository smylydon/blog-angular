import { ReactionButtonCountPipe } from './reaction-button-count.pipe';

describe('ReactionButtonCountPipe', () => {
  it('create an instance', () => {
    const pipe = new ReactionButtonCountPipe();
    expect(pipe).toBeTruthy();
  });
});
